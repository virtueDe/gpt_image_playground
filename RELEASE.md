## v0.7.8-studio.3（2026-09-04）

### 变更
- Studio 设置仅保留 OpenAI 兼容接口与 sub2api（异步）两种服务商类型，并禁止新增配置。
- Studio 配置名称、API URL 和 API Key 默认加载主站当前用户配置且不可修改，服务商类型仍可切换。
- 禁止 Studio 通过配置导入或清除操作覆盖主站配置。

## v0.7.8-studio.2（2026-09-04）

### 变更
- 恢复 Studio 设置入口，默认显示当前用户主站 API 地址和图片分组 Key。
- 修复主站侧图片生成菜单图标在深色模式下颜色不跟随主题的问题。

## v0.7.8-studio.1（2026-09-02）

### 变更
- 新增 Sub2API 站内 Studio 模式，从当前登录用户的可用图片分组同步 API Key，并固定使用 `gpt-image-2`。
- 隐藏外部 API 配置和 Agent 入口，增加返回主站、无可用 Key 与登录失效引导。
- 原始 API Key 仅保留在运行内存，写入本地持久化前会自动清除。
- 增加独立 Studio 容器与仅由 `v*` 标签触发的服务器部署流程。

## v0.7.8（2026-08-28）

### 变更
- 统一服务商类型下拉列表与 API 配置列表的选项行高和操作按钮尺寸，提升界面一致性。
